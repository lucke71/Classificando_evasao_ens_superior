library(RPostgreSQL)
library(dplyr)
# loads the PostgreSQL driver
drv <- dbDriver("PostgreSQL")
# creates a connection to the postgres database
# note that "con" will be used later in each connection to the database
con <- dbConnect(drv, dbname = "lucas_assis",
host = "localhost", port = 5432,
user = "lucas_assis", password = "139755")
tb_exemplo = dbGetQuery(con,"select * from unb_curso_completo")
tb_exemplo = tb_exemplo[sample(1:nrow(tb_exemplo),1000),]
db_example = select(tb_exemplo,-c(co_aluno_situacao,co_ies,co_curso,ano_max,idade,tp_sexo,nacionalidade,cod_municipio_esc,cod_municipio_residencia,cod_municipio_nascimento,cod_municipio_prova))
pasta = "/home/lucas_assis/Desktop/Classificando_evasao_ens_superior/Pacote R classifica evasao/"
setwd(pasta)
rm(con)
rm(drv)
rm(pasta)
rm(tb_exemplo)
source("/home/lucas_assis/Desktop/Classificando_evasao_ens_superior/Funcoes R/Funcao para classificadores.R")
source("/home/lucas_assis/Desktop/Classificando_evasao_ens_superior/Funcoes R/Funcoes para criacao dos dados (sql).R")
package.skeleton(name="Classify.dropout",list=c("classify_dropout","create_dropout_database","db_example"))
library(caret)
?upSample
?train
classify_dropout(db_example,classifier = "CART")
class(cart_fit)
cart_pred
class(cart_pred)
\code{\link{help}}
code{\link{help}}
cart_fit$finalModel
help(package="caret")
